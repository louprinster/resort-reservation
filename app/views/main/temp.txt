                        <p>
                          <b>Rate Breakdown:</b><br>
                          <table>
                          <% (@reservation_item.start_date...@reservation_item.end_date).each do |date| %>
                            <tr>
                              <td><%= "#{date.strftime('%a, %b %d, %Y')}" %></td>
                              <% if (date.strftime("%u").to_i == 5) || (date.strftime("%u").to_i == 6) %>
                                 <td><%="$#{sprintf('%.2f',rental_type.weekend_rate)}" %></td>
                              <% else %>
                                <td><%="$#{sprintf('%.2f',rental_type.weekday_rate)}" %></td>
                              <% end %>
                            </tr>
                          <% end %>
                          </table>                        
                        </p>


  <table class="table">
     <% sum = 0 %>
     <%= (start_date...end_date).each do |date| %>
        <tr>
          <th><%= "#{date.strftime('%a, %b %d, %Y')}" %></th>"
          <% if (date.strftime("%u").to_i == 5) || (date.strftime("%u").to_i == 6) %>
             <td>$<%= "#{sprintf('%.2f',rental_type.weekday_rate)}" %></td>
             <% sum = sum + rental_type.weekday_rate %>
          <% else %>
            <td>$<%= "#{sprintf('%.2f',rental_type.weekend_rate)}" %></td>
            <% sum = sum + rental_type.weekend_rate %>
          <% end %>
        </tr>
      <% end %>
      <% ave_rate = sum/(start_date...end_date).count %>
  </table>


